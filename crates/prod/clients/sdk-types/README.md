# ergo-sdk-types

Shared SDK-facing data types only.

## Doctrine

- No runtime policy or semantic validation.
- No host/supervisor execution logic.
- Keep types transport-safe and serialization-friendly.
